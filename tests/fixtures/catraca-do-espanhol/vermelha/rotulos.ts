/**
 * Fixture VERMELHA da catraca da #603 — a tabela que mora em OUTRO módulo, com
 * UM valor que não existe no dicionário.
 *
 * A catraca tem de REPROVAR apontando `painel.tsx:<linha>` e o texto que falta.
 */
export const ROTULO_DA_ETAPA = {
  concluida: "Concluída",
  semTraducao: "Rótulo que a fixture vermelha deixou sem tradução",
};
